sudo apt install git -y
sudo apt remove git -y
git --version

git config --global user.name "Tay Zar Naung"
git config --global user.email "tzn@mail.com"
git config --list
git config --list --show-origin

git init
git status

git add .
git commit -m "message"

git commit -am "modified"

git log
git log --pretty=oneline
git log --pretty=oneline branchB..branchA	# show commits on branchA thar are not on branchB		
git log --follow <file-name>				# list version history for a file, including renames
git checkout <commit-id>

git tag 0.1.0	VS	git tag -d 0.1.0
git checkout 0.1.0

git reset <file-name>
git reset 	VS	  git reset --hard
git reset <commit-id> 	VS 	git reset --hard <commit-id>

git branch
git branch <new-branch-name>	VS	  git checkout -b <new-branch-name>
git branch -d <branch-name> 	VS	  git branch --delete <branch-name>
git merge <branch-name>
git checkout <branch-name>

git stash
git stash pop
git stash list
git stash drop

git remote add origin git@githuub.com:tayzarnaung/test.git
git remote add origin https://github.com:tayzarnaung/test.git
git remote show origin 
git remote remove origin

git clone git@githuub.com:tayzarnaung/test.git
git clone https://github.com:tayzarnaung/test.git

git pull origin master (or) <branch-name>
git push origin master (or) <branch-name>

git clone pj ../pj2 
cd pj2
git remote show origin  (show URL of pj, origin)
git pull origin master (pull to clone)
(cannot git push oirgin master from clone )

mkdir ~/code/centralism
cd central
git init --bare
git clone . ../app1
git clone centralism app2
cd app1
git remote show origin  (show link for pj)
git remote remove origin
git remote add origin ~/code/centralism (or) /home/wc/code/centralism

-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-
In CLI, a single hyphen can be followed by multiple single-character flags. A double hyphen prefixes a single, multicharacter option.
	tar -czf	=> -czf specifies three single-character flags: create(c), gzip compress(z), file(f) 
	tar --exclude	=> tar interprets it as exclude rather than a combination of e, x, c, l, u, d, e
-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-	-

# How to show current git branch in terminal
# Overwrite in ~/.bashrc to show current git branch in bash prompt
# Restart terminal or type 'source ~/.bashrc' in terminal
parse_git_branch() {
 git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/'
}
if [ "$color_prompt" = yes ]; then
 PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\]\w\[\033[01;31m\]$(parse_git_branch)\[\033[00m\]\$ '
else
 PS1='${debian_chroot:+($debian_chroot)}\u@\h:\w$(parse_git_branch)\$ '
fi
